.DS_Store

# Secrets / env (NUNCA versionar)
.env
.env.*
!.env.example
*.pem
*.key

# Supabase local
.branches
.temp
supabase/.temp/

# Node / Expo (futuro frontend)
node_modules/
.expo/
dist/

# Herramientas de verificación local (Playwright para screenshots; no son del proyecto)
/package.json
/package-lock.json
api/_shot*.mjs

# Estado del editor Claude Design (no aplica fuera del editor)
api/public/design/.image-slots.state.json
